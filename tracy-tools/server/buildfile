intf_libs = # Interface dependencies.
impl_libs = # Implementation dependencies.
#import xxxx_libs += libhello%lib{hello}

lib{tracy-server}: {hxx ixx txx cxx}{**} $impl_libs $intf_libs

# Build options.
#
cxx.poptions =+ "-I$out_root" "-I$src_root"

#obja{*}: cxx.poptions += -DSERVER_STATIC_BUILD
#objs{*}: cxx.poptions += -DSERVER_SHARED_BUILD

# Export options.
#
lib{tracy-server}:
{
  cxx.export.poptions = "-I$out_root" "-I$src_root"
  cxx.export.libs = $intf_libs
}

#liba{tracy-server}: cxx.export.poptions += -DSERVER_STATIC
#libs{tracy-server}: cxx.export.poptions += -DSERVER_SHARED

# For pre-releases use the complete version to make sure they cannot
# be used in place of another pre-release or the final version. See
# the version module for details on the version.* variable values.
#
if $version.pre_release
  lib{tracy-server}: bin.lib.version = "-$version.project_id"
else
  lib{tracy-server}: bin.lib.version = "-$version.major.$version.minor"

# Install into the server/ subdirectory of, say, /usr/include/
# recreating subdirectories.
#
{hxx ixx txx}{*}:
{
  install         = include/server/
  install.subdirs = true
}
